# Repo layout

- `terraform/` — infrastructure code
- `terraform/cloud-init/` — base hardening + optional WireGuard bootstrap YAML
- `docs/` — step-by-step guides
- `examples/` — safe example configs
